interface:
  display_name: "Ajax Model Router"
  short_description: "Claude-bound harness router (CALLER_HARNESS=claude)"
  default_prompt: "Use $model-router with CALLER_HARNESS=claude to emit one ROUTING_DECISION."
