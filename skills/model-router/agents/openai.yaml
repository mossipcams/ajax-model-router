interface:
  display_name: "Ajax Model Router"
  short_description: "Harness-boundary mediation: USE_NATIVE, DELEGATE, or STOP"
  default_prompt: "Use $model-router to emit one ROUTING_DECISION, then USE_NATIVE, DELEGATE, or STOP."
