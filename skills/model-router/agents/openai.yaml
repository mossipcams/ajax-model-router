interface:
  display_name: "Model Router"
  short_description: "Thin control plane: agent, model, risk, scope, verify, fallback"
  default_prompt: "Use $model-router to emit one EXECUTION decision, then execute and verify."
