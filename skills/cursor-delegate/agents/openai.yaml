interface:
  display_name: "Cursor Delegate"
  short_description: "Execute a router-bound Cursor agent"
  default_prompt: "Run model-router first, then use $cursor-delegate only with its EXECUTION decision."
policy:
  allow_implicit_invocation: false
