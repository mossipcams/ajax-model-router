interface:
  display_name: "Cursor Delegate"
  short_description: "Delegate bounded work to Cursor CLI"
  default_prompt: "Run model-router first, then use $cursor-delegate only with its ROUTING_DECISION."
policy:
  allow_implicit_invocation: false
