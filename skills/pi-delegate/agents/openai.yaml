interface:
  display_name: "Pi Delegate"
  short_description: "Execute a router-bound Pi agent"
  default_prompt: "Run model-router first, then use $pi-delegate only with its EXECUTION decision."
policy:
  allow_implicit_invocation: false
