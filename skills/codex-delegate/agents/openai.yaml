interface:
  display_name: "Codex Delegate"
  short_description: "Execute a router-bound Codex agent"
  default_prompt: "Run model-router first, then use $codex-delegate only with its EXECUTION decision."
policy:
  allow_implicit_invocation: false
