interface:
  display_name: "Codex Delegate"
  short_description: "Execute a router-bound Codex lane"
  default_prompt: "Run model-router first, then use $codex-delegate only with its ROUTING_DECISION."
policy:
  allow_implicit_invocation: false
