#!/usr/bin/env bash
set -euo pipefail

# Resolve through symlinks so worktree installs still find canonical libexec.
ROOT="$(cd "$(dirname "$(python3 -c 'import os,sys; print(os.path.realpath(sys.argv[1]))' "${BASH_SOURCE[0]}")")/.." && pwd -P)"
exec python3 "$ROOT/libexec/router_state.py" "$@"
